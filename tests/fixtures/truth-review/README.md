# Truth review fixture

`fixture-set/ground_truth.json` is a short synthetic three-episode draft. Tests generate its
60-second tone WAV at runtime with `scripts/make_audio_fixtures.py`; no audio binary is committed.
